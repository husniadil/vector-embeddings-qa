# Required: OpenAI Configuration
OPENAI_API_KEY=your_openai_api_key_here

# Optional: OpenRouter Configuration (for alternative completion models)
# Only needed if you want to use models from OpenRouter instead of OpenAI
# OPENROUTER_API_KEY=your_openrouter_api_key_here
# OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# Model Configuration
# Default embedding model from OpenAI
EMBEDDING_MODEL=text-embedding-3-small
# Completion model (please pay attention to the model name you are using, whether it is OpenAI's or OpenRouter's)
COMPLETION_MODEL=gpt-4.1-mini